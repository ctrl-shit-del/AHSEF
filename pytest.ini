[pytest]
norecursedirs = datasets metadata results checkpoints .git venv .agents
markers =
    integration: requires real dataset files and runs a data/model/training integration path.
    smoke: performs a short end-to-end training run; select explicitly with -m smoke.

